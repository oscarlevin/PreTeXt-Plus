{
  "name": "app",
  "private": true,
  "devDependencies": {
    "esbuild": "^0.27.4"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --format=esm --outdir=app/assets/builds --public-path=/assets"
  },
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo-rails": "^8.0.20",
    "@pretextbook/web-editor": "^0.0.31",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "overrides": {
    "dompurify": "^3.3.2"
  }
}
